name: Deploy Frontend to VPS

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          script_stop: true
          script: |
            set -e
            APP_DIR="${{ secrets.VPS_APP_DIR }}" 

            echo "📂 Vào thư mục dự án"
            if [ ! -d "$APP_DIR/.git" ]; then
              mkdir -p "$APP_DIR"
              git clone ${{ secrets.REPO_SSH_URL }} "$APP_DIR"
            fi
            cd "$APP_DIR"

            echo "🔄 Đồng bộ code nhánh main"
            git fetch --all
            git checkout main
            git reset --hard origin/main
            git clean -fd

            echo "📝 Ghi file .env.production từ secret"
            echo "${{ secrets.FRONTEND_ENV_PRODUCTION }}" > .env.production

            echo "🧼 Build succeeded. Stopping old container(s)..."
            docker compose -f docker-compose.yml down || true

            echo "🏗️ Building and starting Docker containers (with orphan removal)..."
            docker compose -f docker-compose.yml up --build -d --remove-orphans

            if [ $? -eq 0 ]; then
              echo "✅ Containers built and started successfully!"
            else
              echo "❌ Failed to build/start containers."
              exit 1
            fi

            echo "⏳ Waiting for container to initialize..."
            sleep 20

            echo "🩺 Performing health check..."
            ID=$(docker compose ps -q frontend)
            if [ -z "$ID" ]; then
              echo "❌ Không tìm thấy container ID"
              exit 1
            fi
            echo "🩺 Chờ container healthy (tối đa 120s)..."
            deadline=$((SECONDS+120))
            while [ "$SECONDS" -lt "$deadline" ]; do
              status=$(docker inspect -f '{{.State.Health.Status}}' "$ID" 2>/dev/null || echo starting)
              if [ "$status" = "healthy" ]; then
                echo "✅ Deployment thành công: $status"
                docker image prune -f --filter "until=168h" || true
                exit 0
              elif [ "$status" = "unhealthy" ]; then
                echo "❌ Healthcheck fail: $status"
                docker compose -f "$COMPOSE_FILE" logs --since=10m "$SERVICE" | tail -n 200 || true
                exit 1
              fi
              sleep 3
            done

            echo "❌ Timeout chờ healthy"
            docker compose -f "$COMPOSE_FILE" logs --since=10m "$SERVICE" | tail -n 200 || true
            exit 1
