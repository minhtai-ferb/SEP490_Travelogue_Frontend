name: Deploy Frontend to VPS

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          script_stop: true
          script: |
            set -e
            APP_DIR="${{ secrets.VPS_APP_DIR }}" 

            echo "📂 Vào thư mục dự án"
            if [ ! -d "$APP_DIR/.git" ]; then
              mkdir -p "$APP_DIR"
              git clone ${{ secrets.REPO_SSH_URL }} "$APP_DIR"
            fi
            cd "$APP_DIR"

            echo "🔄 Đồng bộ code nhánh main"
            git fetch --all
            git checkout main
            git reset --hard origin/main
            git clean -fd

            echo "📝 Ghi file .env.production từ secret"
            echo "${{ secrets.FRONTEND_ENV_PRODUCTION }}" > .env.production

            echo "🏗️ Build image Node 22"
            docker compose build --pull

            echo "🚀 Khởi động container"
            docker compose up -d --remove-orphans

            echo "⏳ Chờ container 'frontend' healthy (tối đa ~2 phút)"
            ID=$(docker compose ps -q frontend)
            timeout 120 sh -c '
              while true; do
                status=$(docker inspect -f "{{.State.Health.Status}}" '"$ID"' 2>/dev/null || echo "starting")
                [ "$status" = "healthy" ] && break
                [ "$status" = "unhealthy" ] && exit 1
                sleep 5
              done
            ' && echo "✅ Deploy OK" || (echo "❌ Health check failed" && docker compose ps && docker compose logs --since=5m frontend | tail -n 300 && exit 1)

